# Prueba-RDA-
Prueba para realizar los RDA

## Vista previa local

Para previsualizar la página estática en un navegador, inicia un servidor HTTP simple desde la carpeta del proyecto y abre `http://localhost:8000`:

```bash
python -m http.server 8000
```

Detén el servidor con `Ctrl+C` cuando termines de revisar.
